时间:21:38 2014-8-18

jdk8的新特性
接口可以定义带有方法体的静态方法,可以通过接口名直接调用,但不能实例化.

可以定义关键字为default的非静态的默认方法,在jdk7中有默认访问控制符,但是不能写出default,而且不能再用final修饰,使用子类的对象调用接口中的方法.默认方法可以重写但是不能写default关键字.
接口声明子类实例化,子类没有重写调用接口的方法.有重写调用子类的方法